/*Para criar um objeto utilizamos as chaves envolvendo
as propriedades, que são atribuídas com dois pontos,
por exemplo: , para acessar propriedades esses usamos
o ".", por exemplo:*/

//criando objeto

const person = {
    name: 'Wine',
    age: 21,
    weight: 88.6,
    isAdmin = true 
}
console.log(`${person.name} tem ${person.age} anos`) /*como conseguir ver uma propriedade da variável.*/